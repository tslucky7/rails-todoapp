.container
  %h2 New Comment
  = form_with(model: [ @board, @task, @comment ], local: true) do |f|
    .content-inner
      .field
        = f.label :content, "※一度入力したコメントは編集・削除ができません。ご注意ください。"
        %br/
        = f.text_area :content
      .actions
        = f.submit "Submit", class: "submit-btn"